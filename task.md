# Task Plan
- [x] Add directional buttons to move the pattern image (only if `backgroundImage` is present).
- [x] Update shield image position to x = -170, y = -325 in SVG.
- [x] Move the pattern control buttons higher.
- [x] Change the 3D default position of teamLogo to match the new coordinates (approx x=0.06, y=0.11).

# Intro Animation Implementation Tasks
# Task Plan
- [x] Add directional buttons to move the pattern image (only if `backgroundImage` is present).
- [x] Update shield image position to x = -170, y = -325 in SVG.
- [x] Move the pattern control buttons higher.
- [x] Change the 3D default position of teamLogo to match the new coordinates (approx x=0.06, y=0.11).

# Intro Animation Implementation Tasks

- [x] Create `IntroAnimation.jsx` component.
- [x] Create `IntroAnimation.css` for styling and keyframes.
- [x] Update `App.jsx` to render the transition.
- [x] Verify functionality locally.

# Uniform Coloring and 3D Thumbnails

- [x] Unify color intensity between sleeves and torso.
- [x] Implement enhanced 3D-styled pattern thumbnails.
- [x] Push changes to GitHub.
- [x] Move the pattern control buttons higher.
- [x] Change the 3D default position of teamLogo to match the new coordinates (approx x=0.06, y=0.11).
